package com.logiconnect.platform.channel.entity;

/**
 * Channel membership role matching database check constraint:
 * 'MEMBER', 'MODERATOR', 'ADMIN'
 */
public enum ChannelMemberRole {
    MEMBER,
    MODERATOR,
    ADMIN
}
