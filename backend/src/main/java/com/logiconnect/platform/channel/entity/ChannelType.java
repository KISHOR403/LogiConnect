package com.logiconnect.platform.channel.entity;

/**
 * Organizational channel types matching the database check constraint:
 * 'COMPANY', 'DEPARTMENT', 'TEAM', 'PRIVATE'
 */
public enum ChannelType {
    COMPANY,
    DEPARTMENT,
    TEAM,
    PRIVATE
}
